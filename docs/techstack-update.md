# Techstack & Toolstack Update

## Changes Made

### Techstack.tsx — skills updated to reflect real projects

**Removed:** C/C++ (not used in any project), unused `Mongo` import

**Added:**
- NestJS (`SiNestjs`) — backend framework across all Node.js APIs
- PHP (`SiPhp`) — additional language
- Prisma (`SiPrisma`) — ORM in all Node.js backends
- Redis (SVG) — cache/queue in gabriel & Urbano Vitalino
- AWS (SVG) — S3 storage in dadivas, gabriel, uvas
- Docker (SVG) — containerized deployment across all projects

**Final order:** TypeScript, Node.js, JavaScript, React.js, Next.js, NestJS, PHP, Tailwind CSS, Prisma, PostgreSQL, Redis, AWS, Docker, Python, Git

### Toolstack.tsx — tools updated

**Added:**
- Docker (SVG) — used for local dev and deployment
- Postman (SVG) — API testing
